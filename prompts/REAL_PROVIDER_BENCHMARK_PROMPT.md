# Real Provider Benchmark Prompt

Run a controlled, explicitly budgeted model/provider probe. Do not treat marketing claims as measured truth.

Record: scenario ID, provider/model/version/profile/compiler versions, exact UniversalVideoSpec, compiled prompt, reference bindings, params/duration/resolution, upstream task IDs, raw cost, latency, output artifacts, automated/human scores, failure reason and accepted seconds.

Change one major variable at a time where possible. Never auto-regenerate failed media. A new paid sample is an explicit benchmark attempt under the benchmark budget.

Update ModelProfile recommendations only when sample scope/confidence supports it; preserve conflicting findings.
